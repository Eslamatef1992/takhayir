'use strict';

// Seeds nothing by default — banners are managed entirely from the admin
// panel (Admin > Banners) since the actual creative/photography is up to
// the store owner. This file is a placeholder so the migrations/seeders
// folder stays self-documenting; feel free to delete it.
module.exports = {
  up: async () => {},
  down: async () => {}
};
